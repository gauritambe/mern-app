version: "3.9"

services:
  backend:
    build: ./backend
    container_name: backend
    ports:
      - "8888:8888"
    
  frontend:
    build: ./frontend
    container_name: frontend
    ports:
      - "3000:3000"
